export interface skillsWithProfiency {
  skillId: number;
  name: string;
  description: string;
  pic: string;
  profiency: string;
}

export interface SkillDTO {
  skillId: number;
  name: string;
  description: string;
  pic: string;
}
